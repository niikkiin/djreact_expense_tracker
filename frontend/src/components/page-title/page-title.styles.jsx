import styled from 'styled-components';

const textColor = '#252A32';

export const PageTitleHeading = styled.h1`
  color: ${textColor};
  font-size: 2.8rem;
  font-weight: 600;
  text-transform: capitalize;
`;
