import React from 'react';

// styled components
import {
  CustomButtonContainer 
} from './custom-button.styles';

const CustomButton = () => (
  <CustomButtonContainer>
    Add
  </CustomButtonContainer>
);

export default CustomButton;
