import styled from 'styled-components';

const lineColor = '#F3F3F3';
const textColor = '#323232';

export const SubHeadingContainer = styled.div`
font-size: 2rem;
border-bottom: 1px solid ${lineColor};

  div {
    padding: 1.5rem;
    color: ${textColor};
  }
`;
